# tests/test_data_transformations.py

import pytest
import torch
from temporal.data.transformations import # Replace with actual transformations

# Example test (replace with actual transformations and data)
# def test_transformation_x():
#     data = torch.randn(10, 32)
#     transformed_data = transformation_x(data)
#     assert transformed_data.shape == (10, 32)
#     # Add more assertions to check the transformed values
